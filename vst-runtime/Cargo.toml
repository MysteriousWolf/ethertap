[package]
name = "vst-runtime"
version = "0.1.0"
edition = "2021"
description = "Headless in-process driver for Rust-native nih_plug::Plugin impls — directed test automation, zero IPC"

[dependencies]
nih_plug = { git = "https://github.com/robbert-vdh/nih-plug.git", features = ["vst3"] }

[dev-dependencies]
ethertap = { path = ".." }
