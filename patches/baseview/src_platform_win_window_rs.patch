diff --git a/src/platform/win/window.rs b/src/platform/win/window.rs
index f7c4571..4e955bb 100644
--- a/src/platform/win/window.rs
+++ b/src/platform/win/window.rs
@@ -23,6 +23,7 @@ use raw_window_handle::{
     HasRawDisplayHandle, HasRawWindowHandle, RawDisplayHandle, RawWindowHandle, Win32WindowHandle,
     WindowsDisplayHandle,
 };
+extern crate rwh04;
 
 const BV_WINDOW_MUST_CLOSE: u32 = WM_USER + 1;
 
@@ -582,19 +583,31 @@ pub struct Window<'a> {
 impl Window<'_> {
     pub fn open_parented<P, H, B>(parent: &P, options: WindowOpenOptions, build: B) -> WindowHandle
     where
-        P: HasRawWindowHandle,
+        // Accept rwh 0.4 so that iced_baseview (which pins 0.4) can call this.
+        P: rwh04::HasRawWindowHandle,
         H: WindowHandler + 'static,
         B: FnOnce(&mut crate::Window) -> H,
         B: Send + 'static,
     {
         let parent = match parent.raw_window_handle() {
-            RawWindowHandle::Win32(h) => h.hwnd,
+            rwh04::RawWindowHandle::Win32(h) => h.hwnd,
             h => panic!("unsupported parent handle {:?}", h),
         };
 
         Self::open(true, parent, options, build)
     }
 
+    /// Create a window without a parent and return immediately without running a
+    /// message loop. The caller drives the message loop or embeds the window itself.
+    pub fn open_as_if_parented<H, B>(options: WindowOpenOptions, build: B) -> WindowHandle
+    where
+        H: WindowHandler + 'static,
+        B: FnOnce(&mut crate::Window) -> H,
+        B: Send + 'static,
+    {
+        Self::open(false, null_mut(), options, build)
+    }
+
     pub fn open_blocking<H, B>(options: WindowOpenOptions, build: B)
     where
         H: WindowHandler + 'static,
