index d3684c9..48f3f08 100644
--- a/src/macos/window.rs
+++ b/src/macos/window.rs
@@ -65,7 +65,7 @@ pub(super) struct WindowInner {
     ns_view: id,

     #[cfg(feature = "opengl")]
-    gl_context: Option<GlContext>,
+    pub(super) gl_context: Option<GlContext>,
 }

 impl WindowInner {
@@ -172,6 +172,10 @@ impl<'a> Window<'a> {
         unsafe {
             let _: id = msg_send![handle.ns_view as *mut Object, addSubview: ns_view];
 
+            // NSViewWidthSizable (2) | NSViewHeightSizable (16) — the subview tracks
+            // the parent frame so resize events propagate to the inner iced view.
+            let _: () = msg_send![ns_view, setAutoresizingMask: 18usize];
+
             let () = msg_send![pool, drain];
         }
 
@@ -253,7 +257,8 @@ impl<'a> Window<'a> {
                 rect,
                 NSWindowStyleMask::NSTitledWindowMask
                     | NSWindowStyleMask::NSClosableWindowMask
-                    | NSWindowStyleMask::NSMiniaturizableWindowMask,
+                    | NSWindowStyleMask::NSMiniaturizableWindowMask
+                    | NSWindowStyleMask::NSResizableWindowMask,
                 NSBackingStoreBuffered,
                 NO,
             );
